language: python
python:
    - "2.7.11"
install:
    - "pip install -r requirements.txt --use-mirrors"
    - "pip install -r requirements-dev.txt --use-mirrors"
    - "pip install coveralls"
script: "py.test test/unit_tests --cov=passzero"
after_success:
    - coveralls
