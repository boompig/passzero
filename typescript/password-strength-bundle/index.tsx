import * as React from 'react';
import * as ReactDOM from 'react-dom';
import PasswordStrength from './PasswordStrength';

ReactDOM.render(<PasswordStrength />, document.getElementById('react-root'));
